import 'package:flutter/material.dart';

/// A utility class that holds all the color constants used in the app.
final class AppColors {
  const AppColors._();

  /// Dark grey color, used for text or backgrounds.
  static const Color greyDark = Color(0xFF7F7F7F);

  /// Medium grey color, often used for borders or backgrounds.
  static const Color grey = Color(0xFFBABABA);

  /// Light grey color, suitable for subtle backgrounds.
  static const Color greyLightSecond = Color(0xFFF3F3F3);

  /// Very light grey color, often used for cards or surfaces.
  static const Color greyLightFirst = Color(0xFFF6F6F6);

  /// White color, used for text on dark backgrounds or as a primary background.
  static const Color white = Color(0xFFFFFFFF);

  /// Primary green color, used for buttons and highlights.
  static const Color primaryColor = Color(0xFF0ACF83);

  /// Default black color, used for primary text.
  static const Color defaultColor = Color(0xFF000000);

  /// Accent yellow color, used for highlights or warnings.
  static const Color accent = Color(0xFFFFC120);
}
